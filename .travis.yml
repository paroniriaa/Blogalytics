language: python

branches:
  only:
    - basic-platform-cicd

python:
        - '3.8'
        
install:
        - pip install -r requirements.txt
        - python -m pip install Django
        - python pip install django-ckeditor
        - python pip install Pillow

script:
        - echo 'Run basic test'
        - python manage.py test -v 2
